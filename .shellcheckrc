# Configuration file for shellcheck bash script linter.
# Also configured by vscode workspace settings, see here https://marketplace.visualstudio.com/items?itemName=timonwong.shellcheck

# Allow parsing other referenced files during the check
external-sources=true

# This check for other files can't tell if we cwd'd to another folder, so silence it
disable=1091
