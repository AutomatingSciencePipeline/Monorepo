## Onboarding Tasks
Front-end : 

*Categorize components related, priority, hardness

Tooltip on New Experiments
Verbose Toggle Button -> log level (critical level)
Profile (add Image)
Temp Manual Query (Query to the database)
Stuff that are already in the 
Archive Toggle (Filter -> not used) (Archive Button for each experiment)
Archive All button
Search projects Implementation
Activity Side Bar 
User Constants : Format Validation, once we click the Next Button it should prevent
Scatter plot vs Line plot (switch between them)
Copy Experiment --> We don't need to upload the file again, we just need to configure the parameters (without re-uploading)

Your profile button doesn't do anything

Invite Team
Group -> Having the ability to

Back-end 